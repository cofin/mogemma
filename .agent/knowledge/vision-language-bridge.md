# Knowledge: Vision-Language Bridge

> Flow: vision-language-bridge | Archived

## Patterns

- **Interleaved multimodal inputs:** `generate_multimodal` accepts `list[str | ndarray]` — the most Pythonic API for arbitrary text/image ordering.
- **File:** `src/py/mogemma/vision_model.py`
